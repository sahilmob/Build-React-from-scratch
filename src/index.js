import React from "react";
import ReactDOM from "react-dom";

const element = <h1 title="web dev made simple">Hello World</h1>;
const root = document.getElementById("root");
ReactDOM.render(element, root);
